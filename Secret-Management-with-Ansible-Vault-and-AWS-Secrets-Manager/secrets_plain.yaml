# secrets_plain.yaml
# Plain text secrets — encrypt this file with ansible-vault before committing
# Run: ansible-vault encrypt secrets_plain.yaml --vault-password-file ~/.vault_pass
# NEVER commit this file to version control

db_username: "your-db-username"
db_password: "your-db-password"
api_key: "your-api-key"
app_secret: "your-app-secret"